enum Role {
  SUPER_ADMIN
  ADMIN
  DOCTOR
  PATIENT
}

enum UserStatus {
  ACTIVE
  BLOCKED
  DELETED
}

enum Gender {
  MALE
  FEMALE
  OTHER
}

enum AuthProvider {
  GOOGLE
  CREDENTIAL
}

enum appointmentStatus {
  PENDING
  CONFIRMED
  CANCELLED
  ONGOING
  COMPLETE
}

enum paymentStatus {
   UNPAID
   PAID
   FAILED
   CANCELLED
   REFUNDED
}
